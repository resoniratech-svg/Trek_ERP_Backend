generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:root@127.0.0.1:5433/erp_backend_restored"
}

model Test {
  id Int @id @default(autoincrement())
  name String
}
