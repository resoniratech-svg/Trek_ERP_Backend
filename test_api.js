const axios = require('axios');
async function run() {
  try {
    // Need logged in token. Actually, wait. I can't do this easily without a token.
    // I can just query the DB. It's the exact same query.
  } catch (e) {
  }
}
run();
